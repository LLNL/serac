ksp:
  converged_reason:
  # type: fgmres
  type: fgmres
  norm_type: preconditioned
  rtol: 1e-5

snes:
  converged_reason:

nestpc:
  type: fieldsplit
  fieldsplit:
    detect_saddle_point:
    type: schur
    schur_fact_type: diag
    schur_precondition: a11
    # type: multiplicative
nestfieldsplit:
  0:
    ksp_max_it: 10
  1:
    pc_type: jacobi
    # pc_type: jacobi
