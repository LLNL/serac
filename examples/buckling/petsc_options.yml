ksp:
  converged_reason:
  # type: fgmres
  type: gmres
  norm_type: unpreconditioned
  rtol: 1e-5

snes:
  converged_reason:

nestpc:
  type: fieldsplit
  fieldsplit:
    # type: schur
    # schur_fact_type: diag
    # schur_precondition: a11
    type: multiplicative
nestfieldsplit:
  # 0:
    # ksp_type: preonly
    # mg_coarse_pc_type: cholesky
  1:
    pc_type: jacobi
    # pc_type: jacobi
